name: CI
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:
jobs:
  build:
    runs-on: macos-12
    steps:
      - uses: actions/checkout@v4
      - name: Run a multi-line script
        run: |
          clang --version
          g++ --version
          make --version
          zlib --version
          brew list
